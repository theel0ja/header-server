{
  "name": "header-server",
  "version": "0.0.1",
  "description": "Simple HTTP server that prints headers of the HTTP request as JSON",
  "main": "index.js",
  "scripts": {
    "start": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Elias Ojala",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
